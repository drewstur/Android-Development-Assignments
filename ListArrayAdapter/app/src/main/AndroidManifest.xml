<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="faber.drew.cis46300.listarrayadapter" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_sf"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Bridge"
            android:label="@string/title_activity_bridge" />
        <activity
            android:name=".Trolley"
            android:label="@string/title_activity_trolley" />
        <activity
            android:name=".Wharf"
            android:label="@string/title_activity_wharf" />
    </application>

</manifest>
